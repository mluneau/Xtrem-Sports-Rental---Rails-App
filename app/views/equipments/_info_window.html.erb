<div class="container">
  <h5><strong><%= link_to equipment.title, equipment_path(equipment) %></strong></h5>
  <% if equipment.photo.attached? %>
            <%= cl_image_tag equipment.photo.key, height: 300, width: 400 %>
          <% else %>
            <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/skateboard.jpg", style="width: 120px; height: 120px;"/>
          <% end %>
  <div>
  <br>
    <p>Rent it for: <strong><%= equipment.daily_price %>€ /day</strong></p>
    <p>Owner: <strong><%= equipment.user.first_name %></strong> 😛</p>
  </div>
</div>